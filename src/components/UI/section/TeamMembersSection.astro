---
import CompareCard from '../cards/CompareCard.astro';

const compareItems = [
  {
    before: '/images/Comparision/compare1_1.avif',
    after:  '/images/Comparision/compare1_2.avif',
    altBefore: 'Before',
    altAfter:  'After with VyBrows',
  },
  {
    before: '/images/Comparision/compare2_1.avif',
    after:  '/images/Comparision/compare2_2.avif',
    altBefore: 'Before',
    altAfter:  'After with VyBrows',
  },
  {
    before: '/images/Comparision/compare3_1.avif',
    after:  '/images/Comparision/compare3_2.avif',
    altBefore: 'Before',
    altAfter:  'After with VyBrows',
  },
];
---

<style is:global>
  @import url('https://fonts.googleapis.com/css2?family=Lora:wght@400;700&display=swap');
</style>

<section class="px-6 py-16" style="background: var(--color-bg);">
  <div class="max-w-6xl mx-auto">
    <h2 
      class="text-4xl md:text-5xl font-bold text-center mb-8" 
      style="color: var(--color-text-secondary); font-family: 'Lora', serif;"
    >
      {({
        en: "Your Beauty — Our Commitment",
        vi: "Vẻ Đẹp Của Bạn — Cam Kết Của Chúng Tôi",
        ja: "あなたの美しさ — 私たちの約束",
        ko: "당신의 아름다움 — 우리의 약속",
        es: "Tu Belleza — Nuestro Compromiso"
      })[(Astro.props?.lang || Astro.url.pathname.split('/')[1] || 'en') as 'en' | 'vi' | 'ja' | 'ko' | 'es']}
    </h2>

    <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-[20px]">
      {compareItems.map(item => (
        <CompareCard
          before={item.before}
          after={item.after}
          altBefore={item.altBefore}
          altAfter={item.altAfter}
          width={300}
          height={300}
        />
      ))}
    </div>

  </div>
</section>