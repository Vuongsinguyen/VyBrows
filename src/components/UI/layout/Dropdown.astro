---
export interface Props {
  label: string;
  items: Array<{
    href: string;
    title: string;
    description: string;
  }>;
  class?: string;
}

const { label, items, class: customClass = '' } = Astro.props;
---

<div class="hidden md:flex w-full justify-center" style="position: static;">
  <div class="group dropdown-container w-full" style="position: static;"> 
  <button
    class="dropdown-trigger px-3 py-2 text-sm font-medium transition-colors flex items-center font-lora font-bold"
    style="font-size:16px; color: var(--color-black); position: static;"
  >
    <span class="whitespace-nowrap font-lora font-bold" style="color: var(--color-black2white); font-family: 'Lora', serif;">
      {label}
    </span>
    <svg class="ml-1 h-4 w-4 transition-transform group-hover:rotate-180" fill="none" viewBox="0 0 24 24" stroke="var(--color-stroke)" style="stroke: var(--color-stroke);">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
    </svg>
  </button>

  <div class="dropdown-menu absolute top-full shadow-lg opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-200 z-[9999] px-2 py-2 grid place-items-center w-fit"
  style="box-shadow: var(--color-shadow); background-color: #002319; border-radius: 20px; margin: -10px; left: 50%; transform: translateX(-50%); min-width: 800px; max-width: 1100px; width: 100%;"
  >
  <div class="px-4 py-4 grid gap-4">
      <div class="grid grid-cols-3 gap-4 justify-center text-center">
        {items.map((item) => (
          <a
            class="block transition-colors group/item px-4 py-3 rounded-md text-left whitespace-nowrap font-lora font-bold"
            style="font-size:16px; color: var(--color-black2white); font-family: 'Lora', serif;"
            href={item.href}
          >
            <div class="flex items-center gap-2">
              <span class="mr-2 flex-shrink-0 flex items-center justify-center">
                <span class="inline-flex items-center justify-center rounded-full border" style="width: 38px; height: 38px; border-width: 1px; border-color: rgba(255, 255, 255, 0.35);">
                  <svg width="20" height="20" viewBox="0 0 20 20" fill="none" xmlns="http://www.w3.org/2000/svg">
                    <circle cx="10" cy="10" r="8" stroke="var(--color-black2white)" stroke-width="2" fill="none" />
                    <path d="M7.5 10.5l2 2 4-4" stroke="var(--color-black2white)" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" />
                  </svg>
                </span>
              </span>
              <div class="flex-1 min-w-0">
                <div class="font-medium mb-1 transition-colors text-left min-w-0" style="color: var(--color-black2white); font-size:16px;">
                  {item.title}
                </div>
              </div>
            </div>
          </a>
        ))}
      </div>
    </div>
  </div>
</div>

<style>
  .dropdown-container {
    display: inline-block;
    position: relative;
  }
  .dropdown-menu {
    min-width: 220px;
    width: auto;
    right: 0;
    left: auto;
    position: absolute;
    z-index: 99999;
    padding: 0.5rem 1.5rem;
  }
</style>